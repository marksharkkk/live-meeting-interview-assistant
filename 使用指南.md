# 会议通 - 快速使用指南

## 第一步：配置 API Key

打开 `backend\.env` 文件，填入您的 API Key：

```
OPENAI_API_KEY=在这里填入您的API Key
OPENAI_API_BASE=https://api.deepseek.com/v1    # 如果用DeepSeek
OPENAI_MODEL=deepseek-chat                       # 对应的模型名称
```

**支持的 API 平台：**

| 平台 | Base URL | 推荐模型 | 获取地址 |
|------|----------|----------|----------|
| DeepSeek | https://api.deepseek.com/v1 | deepseek-chat | https://platform.deepseek.com |
| OpenAI | https://api.openai.com/v1 | gpt-4o | https://platform.openai.com |
| Kimi | https://api.moonshot.cn/v1 | moonshot-v1-8k | https://platform.moonshot.cn |

## 第二步：启动软件

首次使用时先双击 `install.bat`，它会从锁文件创建项目专属的 Python 和 Node 环境。

双击 `start.bat` 即可一键启动。

也可以在项目目录运行 `volta run npm start`。后端由桌面端自动启动并建立受保护的本地连接，不应单独启动。

## 第三步：打开提词器

1. 设置窗口打开后，点击 **打开提词器** 按钮
2. 提词器会悬浮在屏幕右上角
3. 将提词器拖放到合适的位置（会议窗口旁边）

## 第四步：开始使用

在提词器窗口中：

1. 点击 **Start Listening** - 开始监听会议语音
2. 停止监听后点击“生成答案”，系统根据已转录的问题生成英文答案
3. 答案显示在绿色区域，可以直接照着回答

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + Shift + H` | 显示/隐藏提词器 |
| `Ctrl + Shift + M` | 最小化/恢复设置窗口 |
| `Ctrl + H` | 开启/关闭隐私模式 |
| `Esc` | 开启隐私模式 |

## 使用流程示例

### 场景：线上会议

```
┌─────────────────────────────────────┐
│         腾讯会议/飞书                │
│                                     │
│  领导：这个项目的技术方案是什么？     │
│                                     │
└─────────────────────────────────────┘
         ↓ 语音被识别
┌──────────────────────────┐
│  🟢 检测到的问题：         │
│  这个项目的技术方案是什么？ │
│                          │
│  💡 建议回答（英文）：     │
│  Our technical approach..│
│  1. Architecture...      │
│  2. Tech stack...        │
└──────────────────────────┘
      ↑ 提词器悬浮窗
```

## 注意事项

1. **首次使用前**：务必先在设置页面测试 API 连接是否正常
2. **提词器位置**：建议放在会议软件旁边，方便边看边回答
3. **语音环境**：尽量确保麦克风能清晰收录会议声音
4. **网络要求**：语音识别使用本地模型，但 AI 生成答案需要联网（调用 API）
5. **答案语言**：默认生成英文答案，适合需要用英文回答的场景
6. **隐私模式**：不同会议软件的捕获方式不同，正式使用前请在目标软件中测试共享画面
7. **模型位置**：Whisper base 模型位于项目的 `backend/whisper_models/base`，随项目移动

## 常见问题

**Q: 提词器不显示答案？**
A: 检查 API Key 是否正确配置，点击"测试连接"验证。

**Q: 语音识别不准确？**
A: 确保麦克风正常工作，可以在安静环境下测试。

**Q: 如何暂停监听？**
A: 点击"Stop Listening"或使用快捷键隐藏提词器。
